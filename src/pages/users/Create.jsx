const Create = () => (
  <div>
    <h1 className="text-2xl font-bold text-main">Create User</h1>
    <p className="text-sm text-muted-custom mt-1">Add a new user to the system.</p>
  </div>
);

export default Create;
